<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.teams.TeamsFragment">



    <GridView
        android:id="@+id/allTeamsGrid"
        android:fillViewport="true"
        android:layout_gravity="top"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:horizontalSpacing="15dp"
        android:numColumns="2"
        android:verticalSpacing="15dp"
        android:gravity="center_horizontal"
        android:scrollingCache="true"
        android:alwaysDrawnWithCache="true"
        android:scrollbarStyle="outsideInset"
        android:padding="18dp"
        android:paddingLeft="15dp"
        android:paddingRight="15dp"
        android:paddingBottom="15dp"
        android:layout_marginBottom="40dp"

        />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:src="@drawable/ic_baseline_add_24"
        android:contentDescription="@string/new_game_result"
        app:layout_constraintBottom_toBottomOf="@+id/allTeamsGrid"
        app:layout_constraintEnd_toEndOf="@+id/allTeamsGrid"
        android:layout_margin="34dp"
        />


</androidx.constraintlayout.widget.ConstraintLayout>